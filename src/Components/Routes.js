import React from "react";
import { View, Text } from "react-native-web";

const Router = () => {
  return (
    <View>
      <Text>welcome in routing</Text>
    </View>
  );
};

export default Router;
